# repricer-api-core

This is using typescript and outputted files will be in /dist

## Dev
Run `npm run dev` to have auto-detecting changes on the dev server.

## Husky
If repository already exists for you, you'll need to run `npm run prepare` to set up husky. Otherwise, on fresh installs there should be no issue. 



