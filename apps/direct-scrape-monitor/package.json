{
  "name": "direct-scrape-monitor",
  "version": "1.0.0",
  "description": "Lightweight scrape monitor - hits Net32 API via proxy and logs results to Datadog",
  "main": "./dist/main.js",
  "scripts": {
    "dev": "ts-node-dev -r dotenv/config --respawn --transpile-only src/main.ts",
    "start": "node dist/main.js",
    "build": "tsc"
  },
  "license": "ISC",
  "dependencies": {
    "axios": "^0.27.2",
    "dotenv": "^16.0.1",
    "knex": "^3.1.0",
    "mysql2": "^3.11.0",
    "zod": "^4.0.8"
  },
  "devDependencies": {
    "@types/node": "^24.0.8",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3"
  }
}
