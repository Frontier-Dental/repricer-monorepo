# Front-end (V2 Algo)

This front-end is just for the V2 algo and is embedded in the existing app, served at the /vite route in the express server. You can run it locally with `npm run dev`. On build in CI, it is packaged. 

